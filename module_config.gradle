ext {
    versionName = "1.0.0"
    versionCode = 1
    // Sdk and tools
    // Sdk and tools
    minSdkVersion = 21
    targetSdkVersion = 30
    compileSdkVersion = 30
    buildToolsVersion = '30.0.3'

    gradleVersion = '3.1.2'

    glideVersion = '4.7.1'
    supportLibraryVersion = '27.1.1'
    okhttp3Version = '3.11.0'
//    aarImageLoaderVersion = "1.3.6-SNAPSHOT"
//    aarImageLoaderVersion = "1.3.2" //zhengshi banben

    if (uploadRelease == 'false') {
        aarImageLoaderVersion = "1.0.7.1-SNAPSHOT"
    } else {
        aarImageLoaderVersion = "1.1.1"
    }
}